defmodule StringCompressionTest do
  use ExUnit.Case
  alias StringCompression

  # @params "aaabbbccc"

  # test "compression/1" do
  #   assert StringCompression.compression(@params) == "a3b3c3"
  # end

  # test "compression/1 when is error" do
  #   assert StringCompression.compression(@params) == "a4b3c3"
  # end
end
